import { DisplayArrowPipe } from './display-arrow.pipe';

describe('DisplayArrowPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayArrowPipe();
    expect(pipe).toBeTruthy();
  });
});
